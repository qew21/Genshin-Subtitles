<Window x:Class="GI_Subtitles.Views.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Update_Title}"
        Width="680"
        Height="520"
        MinWidth="520"
        MinHeight="400"
        Icon="../Resources/mask.ico"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{DynamicResource Update_AvailableHeading}"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0"
                       Text="{DynamicResource Update_VersionLabel}"
                       FontWeight="SemiBold" Margin="0,0,10,6"/>
            <TextBlock x:Name="VersionValue" Grid.Row="0" Grid.Column="1" Margin="0,0,0,6"/>
            <TextBlock Grid.Row="1" Grid.Column="0"
                       Text="{DynamicResource Update_PublishedLabel}"
                       FontWeight="SemiBold" Margin="0,0,10,0"/>
            <TextBlock x:Name="PublishedValue" Grid.Row="1" Grid.Column="1"/>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0"
                       Text="{DynamicResource Update_ReleaseNotesLabel}"
                       FontWeight="SemiBold"
                       Margin="0,0,0,8"/>
            <TextBox x:Name="ReleaseNotesTextBox"
                     Grid.Row="1"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     Padding="12"
                     FontSize="14"
                     Background="#FFF8F8F8"
                     BorderBrush="#FFD0D0D0"/>
        </Grid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,18,0,0">
            <Button Content="{DynamicResource Update_InstallButton}"
                    MinWidth="120" Height="34" Padding="12,4"
                    IsDefault="True" Click="InstallButton_Click"/>
            <Button Content="{DynamicResource Update_IgnoreButton}"
                    MinWidth="120" Height="34" Padding="12,4"
                    Margin="10,0,0,0" Click="IgnoreButton_Click"/>
            <Button Content="{DynamicResource Update_LaterButton}"
                    MinWidth="90" Height="34" Padding="12,4"
                    Margin="10,0,0,0" IsCancel="True" Click="LaterButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
